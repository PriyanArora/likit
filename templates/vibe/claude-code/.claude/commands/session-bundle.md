Regenerate the paste-ready Likit context bundle.

The bundle combines the tool guide, `ProjectSummary.md`, `Progress.md`, and the command list into one block that can be pasted at the start of a fresh chat. Tell the user the canonical way to regenerate it is the terminal command `npx likit session-bundle` (it rewrites the entry file for chat tools). If they want it now, assemble the current `ProjectSummary.md` + `Progress.md` + command list and output it as a single copy-paste block.
