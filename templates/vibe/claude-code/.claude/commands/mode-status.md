Show the Likit configuration for this project.

Read `.likit/config.json` and report: mode (Mentored/Vibe), AI tool, project type, team vs solo, and Likit version. If `.likit/config.json` is missing, tell the user to run `npx likit init`. Report only — change nothing.
