Run an architecture cross-check of the current plan.

Read `ProjectSummary.md`, `BuildFlow.md`, and `_fill_manifest.md`. Audit for: contradictions across decisions, missing definitions, scope gaps, phases with no real proof, and any secret/config handling that breaks the red lines. Then check the gate ledger: run `npx likit doctor` and report any failure (gate integrity, commit convention, hardcoded secrets). Report findings as a numbered list with a severity and a concrete fix for each. This is review only — do not write the user's implementation code. End with: "Resolve these before advancing, or confirm each is acceptable."
